{
  "build": {
    "builder": "NIXPACKS",
        "buildCommand": "COMPOSER_ALLOW_SUPERUSER=1 composer install --no-dev --optimize-autoloader --ignore-platform-req=ext-gd && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "php artisan view:clear && php artisan migrate --force --path=database/migrations/2026_07_12_000001_add_vistas_to_propiedades_table.php && php artisan serve --host=0.0.0.0 --port=$PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
